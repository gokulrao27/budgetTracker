import { NextRequest } from 'next/server';import { actor,json,safe } from '@/lib/api';import { store,AppError } from '@/lib/store';export async function POST(req:NextRequest,{params}:{params:Promise<{id:string}>}){try{const u=await actor();if(!u)throw new AppError('Login required',401);const {reason}=await req.json();return json(await store.rejectPayment(u,(await params).id,reason||'Not approved'))}catch(e){return safe(e)}}
