'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Change(){const[e,setE]=useState('');const[loading,setLoading]=useState(false);const r=useRouter();async function submit(ev:React.FormEvent<HTMLFormElement>){ev.preventDefault();setLoading(true);setE('');const fd=new FormData(ev.currentTarget);const res=await fetch('/api/auth/change-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password:fd.get('password')})});const j=await res.json().catch(()=>({error:'Password change failed'}));setLoading(false);if(!res.ok)setE(j.error||'Password change failed');else r.replace('/')}return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#fff8ec,#fbf6ee_42%,#efe1d3)] p-4"><form method="post" action="/api/auth/change-password" onSubmit={submit} className="surface w-full max-w-md space-y-5 rounded-[2rem] p-7"><p className="eyebrow">First login security</p><h1 className="font-display text-4xl font-bold">Create your new password</h1><p className="text-[var(--muted)]">Temporary passwords expire after this change.</p><label className="field"><span>New password</span><input className="input" name="password" type="password" minLength={8} maxLength={128} autoComplete="new-password" required/></label><button disabled={loading} className="btn w-full">{loading?'Saving...':'Save password'}</button>{e&&<p role="alert" className="rounded-2xl bg-[#fde8e8] p-3 text-[#9b1c1c]">{e}</p>}</form></main>}
