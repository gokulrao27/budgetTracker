'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Change(){const[e,setE]=useState('');const[loading,setLoading]=useState(false);const r=useRouter();async function submit(ev:React.FormEvent<HTMLFormElement>){ev.preventDefault();setLoading(true);setE('');const fd=new FormData(ev.currentTarget);const res=await fetch('/api/auth/change-password',{method:'POST',body:JSON.stringify({password:fd.get('password')})});const j=await res.json();setLoading(false);if(!res.ok)setE(j.error);else r.push('/')}return <main className="grid min-h-screen place-items-center bg-[radial-gradient(circle_at_top,#831843,#050205_58%)] p-4"><form onSubmit={submit} className="hero-card w-full max-w-md space-y-5 p-7"><p className="eyebrow">First login security</p><h1 className="text-4xl font-black">Create your new password</h1><p className="text-white/60">Temporary passwords expire after this change.</p><label className="field"><span>New password</span><input className="input" name="password" type="password" minLength={8} maxLength={128} autoComplete="new-password" required/></label><button disabled={loading} className="btn w-full">{loading?'Saving...':'Save password'}</button>{e&&<p role="alert" className="rounded-2xl bg-rose-500/15 p-3 text-rose-100">{e}</p>}</form></main>}
