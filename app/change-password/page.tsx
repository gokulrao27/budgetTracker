'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';export default function Change(){const[e,setE]=useState('');const r=useRouter();async function submit(ev:React.FormEvent<HTMLFormElement>){ev.preventDefault();const fd=new FormData(ev.currentTarget);const res=await fetch('/api/auth/change-password',{method:'POST',body:JSON.stringify({password:fd.get('password')})});const j=await res.json();if(!res.ok)setE(j.error);else r.push('/')}return <main className="min-h-screen grid place-items-center p-4"><form onSubmit={submit} className="card p-8 max-w-md w-full space-y-4"><h1 className="text-3xl font-bold">Create your new password</h1><input className="input" name="password" type="password" minLength={8}/><button className="btn">Save password</button>{e&&<p>{e}</p>}</form></main>}
