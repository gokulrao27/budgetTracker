'use client';import { useState } from 'react';import { useRouter } from 'next/navigation';
export default function Login(){const[err,setErr]=useState('');const[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const fd=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',body:JSON.stringify({name:fd.get('name'),password:fd.get('password')})});const j=await res.json();setLoading(false);if(!res.ok)setErr(j.error);else r.push(j.mustChangePassword?'/change-password':'/')}return <main className="min-h-screen grid place-items-center p-4 bg-[radial-gradient(circle,#881337,#05030a_60%)]"><form onSubmit={submit} className="card p-8 w-full max-w-md space-y-4"><h1 className="text-3xl font-black">Sign in</h1><input className="input" name="name" placeholder="Name"/><input className="input" name="password" type="password" placeholder="Password"/><button disabled={loading} className="btn w-full">{loading?'Signing in...':'Enter portal'}</button>{err&&<p className="text-rose-300">{err}</p>}</form></main>}
