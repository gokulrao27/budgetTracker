'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Login(){const[err,setErr]=useState('');const[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setErr('');const fd=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:fd.get('name'),password:fd.get('password')})});const j=await res.json().catch(()=>({error:'Login failed'}));setLoading(false);if(!res.ok)setErr(j.error||'Login failed');else r.replace(j.mustChangePassword?'/change-password':'/')}return <main className="grid min-h-screen place-items-center overflow-hidden bg-[radial-gradient(circle_at_top,#fff8ec,#fbf6ee_42%,#efe1d3)] p-4"><form method="post" action="/api/auth/login" onSubmit={submit} className="surface w-full max-w-md space-y-5 rounded-[2rem] p-7"><p className="eyebrow">Private wedding portal</p><h1 className="font-display text-4xl font-bold leading-none">Welcome back.</h1><p className="text-[var(--muted)]">Sign in with your name and private password. Passwords are posted securely and never placed in the URL.</p><label className="field"><span>Name</span><input className="input" name="name" autoComplete="name" required placeholder="Gokul"/></label><label className="field"><span>Password</span><input className="input" name="password" type="password" autoComplete="current-password" required placeholder="••••••••"/></label><button disabled={loading} className="btn w-full">{loading?'Opening portal...':'Enter portal'}</button>{err&&<p role="alert" className="rounded-2xl bg-[#fde8e8] p-3 text-[#9b1c1c]">{err}</p>}</form></main>}
