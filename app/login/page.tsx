'use client';
import { useState } from 'react';
import { useRouter } from 'next/navigation';
export default function Login(){const[err,setErr]=useState('');const[loading,setLoading]=useState(false);const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setErr('');const fd=new FormData(e.currentTarget);const res=await fetch('/api/auth/login',{method:'POST',body:JSON.stringify({name:fd.get('name'),password:fd.get('password')})});const j=await res.json();setLoading(false);if(!res.ok)setErr(j.error);else r.push(j.mustChangePassword?'/change-password':'/')}return <main className="grid min-h-screen place-items-center overflow-hidden bg-[radial-gradient(circle_at_top,#9f1239,#130611_45%,#040205)] p-4"><form onSubmit={submit} className="hero-card w-full max-w-md space-y-5 p-7"><p className="eyebrow">Private wedding portal</p><h1 className="text-4xl font-black leading-none">Enter the celebration.</h1><p className="text-white/60">Sign in with your name and private password. No email required.</p><label className="field"><span>Name</span><input className="input" name="name" autoComplete="name" required placeholder="Gokul"/></label><label className="field"><span>Password</span><input className="input" name="password" type="password" autoComplete="current-password" required placeholder="••••••••"/></label><button disabled={loading} className="btn w-full">{loading?'Opening portal...':'Enter portal'}</button>{err&&<p role="alert" className="rounded-2xl bg-rose-500/15 p-3 text-rose-100">{err}</p>}</form></main>}
