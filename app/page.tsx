import { redirect } from 'next/navigation';import { currentSession } from '@/lib/session';import { store } from '@/lib/store';import { budgetSummary } from '@/lib/domain';import { CategoryPie,ContributionBar } from '@/components/Charts';
const rupee=(n:number)=>`₹${n.toLocaleString('en-IN')}`;
export default async function Home(){await store.bootstrap();const s=await currentSession();if(!s)redirect('/login');const me=store.requireUser(s.userId);if(me.must_change_password)redirect('/change-password');const budget=budgetSummary(store.totalBudget,store.expenses);const mine=store.summary(me);return <main className="min-h-screen bg-[radial-gradient(circle_at_top,#881337,#05030a_55%)] p-4 md:p-8"><nav className="flex justify-between"><b>Wedding Friends Portal</b><form action="/api/auth/logout" method="post"><button className="btn2">Logout</button></form></nav><section className="py-16"><p className="text-rose-300">Private celebration community</p><h1 className="text-5xl md:text-7xl font-black">A cinematic home for the wedding crew.</h1><p className="mt-4 max-w-2xl text-white/70">Friends, memories, contributions, expenses, approvals, and photo moments in one secure portal.</p></section><section><h2 className="text-2xl font-bold mb-4">Friends</h2><div className="flex gap-4 overflow-x-auto pb-4">{store.users.map(u=><div className="card min-w-64 p-4" key={u.id}><div className="h-44 rounded-2xl bg-gradient-to-br from-rose-700 to-slate-900"/><h3 className="mt-3 text-xl font-bold">{u.name}</h3><p className="text-white/60">{u.bio||u.role}</p></div>)}</div></section><section className="grid md:grid-cols-3 gap-4 my-8"><div className="card p-5"><p>Total budget</p><b className="text-3xl">{rupee(budget.totalBudget)}</b></div><div className="card p-5"><p>Total spent</p><b className="text-3xl">{rupee(budget.totalSpent)}</b></div><div className="card p-5"><p>Remaining budget</p><b className="text-3xl">{rupee(budget.remainingBudget)}</b></div></section><section className="grid md:grid-cols-2 gap-6"><div className="card p-5"><h2 className="text-xl font-bold">Expense chart</h2><CategoryPie data={Object.entries(budget.byCategory).map(([name,value])=>({name,value}))}/></div><div className="card p-5"><h2 className="text-xl font-bold">My contribution</h2><p>Required {rupee(mine.required)} · Approved {rupee(mine.approvedPaid)} · Pending {rupee(mine.pendingAmount)} · Remaining {rupee(mine.remaining)}</p><ContributionBar paid={mine.approvedPaid} remaining={mine.remaining}/></div></section>{['ADMIN','SUPER_ADMIN'].includes(me.role)&&<a className="btn inline-block mt-8" href="/admin">Open admin dashboard</a>}</main>}
